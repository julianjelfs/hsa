a.btn.right(href='/circle/new') New Circle
h1 This is the list of circles

table.table
    th Name
    th Description
    th
    th
    tr(ng-repeat="circle in circles")
        td {{circle.name}}
        td {{circle.description}}
        td
            button.btn(ng-click='delete(circle)') Delete
        td
            a.btn(href='/circle/edit/{{circle._id}}') Edit