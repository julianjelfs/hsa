.small-offset-1.small-10.columns
    .news
      h4 {{event.title}} on {{event.date | date}} from {{event.start}} to {{event.end}}
      p {{event.description}} 
      p 
        a.button.success(href="/volunteer") Volunteer!