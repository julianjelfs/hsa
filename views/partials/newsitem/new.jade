h3 {{viewTitle}}

form(ng-submit="submit()")
    div
        label Title
        input(type="text", ng-model="newsitem.title")

    div
        label Description
        textarea(rows="10", ng-model="newsitem.description")

    div
        button.btn(type="submit") {{buttonText}}